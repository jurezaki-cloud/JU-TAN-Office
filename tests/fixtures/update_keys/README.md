# Test-only Ed25519 public keys (fixtures).
# NEVER package these into production trust material.
# Private seeds for tests are generated in-memory or loaded from *.seed files
# that are gitignored when created locally; deterministic seeds live in tests.
